# Content Presets

Each preset provides starter content for a theme/use-case.

Apply via CLI:

```bash
php bin/atoll preset:list
php bin/atoll preset:apply business
```

Use `--force` to overwrite existing files.
Use `--no-theme` to keep current `appearance.theme`.

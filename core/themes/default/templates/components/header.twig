<header class="site-header">
  <div class="container row">
    <a class="brand" href="{{ url('/') }}">atoll-cms</a>
    <nav>
      <ul class="nav">
        {% for item in navigation %}
          <li><a href="{{ url(item.url|default('/')) }}">{{ item.label }}</a></li>
        {% endfor %}
      </ul>
    </nav>
    <a class="admin-link" href="{{ url('/admin') }}">Admin</a>
  </div>
</header>
